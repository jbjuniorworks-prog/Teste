# Consumed by docker-compose for the Postgres + Redis services.
# Copy this file to .env and adjust as needed (.env is gitignored).
POSTGRES_USER=chatsample
POSTGRES_PASSWORD=chatsample
POSTGRES_DB=chatsample
POSTGRES_PORT=5432
REDIS_PORT=6379
