[project]
name = "chat-sample-mcp"
version = "0.1.0"
description = "MCP server (HTTP proxy) for the Chat Sample API"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]
