const apiClient = {
  get: async () => {
    throw new Error('apiClient.get ainda não implementado');
  },
  post: async () => {
    throw new Error('apiClient.post ainda não implementado');
  },
};

export default apiClient;
